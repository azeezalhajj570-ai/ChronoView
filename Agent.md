Create an Android app named **ChronoView** using **Java or Kotlin in Android Studio**.

The app should be a premium watch store.

Core features:

* Display watch products in a grid or list.
* Each watch should show image, name, brand, price, and rating.
* Search watches by name or brand.
* Filter watches by category: Luxury, Sport, Casual.
* Open a product detail screen showing large images, description, price, rating, and Add to Cart button.
* Add products to a shopping cart.
* Show cart items with quantity controls (+ / -).
* Calculate and display total price.
* Add a UI-only Checkout button.
* Add an AI-style watch description generator using local template-based logic.
* Add an image carousel on the product detail screen.
* Store cart data locally using SQLite, Room Database, SharedPreferences, or DataStore.

Style:

* Primary color: #263859
* Background color: #F4F4F6
* Accent color: #7661E6
* Use an elegant premium design.
* Use card-based layouts, rounded corners, whitespace, and smooth animations.
* Use Android UI components such as TextView, Button, EditText, ImageView, RecyclerView or Jetpack Compose components.

Important:
This must be a native Android app, not a web app.
Do not use localStorage, sessionStorage, HTML, CSS, or JavaScript.
Generate Android Studio-ready code.
